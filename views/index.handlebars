<div class="container">
    <div class="row">
        <div class="col s12">
            <p class="center-align">Keep track of your quarantine TV escapades. If there is a show you are determined
                to binge during lockdown, add it using the form below. After you've braved all of it's seasons on the
                couch, click the button to add it to your favorites list. If you didn't like the show, you can remove it.
                Otherwise, you can see the fruits of your labor in all their glory.</p>
        </div>
    </div>
    <div class="row">
        <div class="col s6 m6 l6">
            <h4 class="center-align">Watchlist</h4>
            {{#each shows}}
            {{#unless watched}}
            <div id={{id}}>
                <p>Title: {{title}}</p>
                <p>Seasons: {{seasons}}</p>
                <p>Approximate TV time: {{runtime}} hours</p>
                <button class="binged">Show Binged!</button>
            </div>
            {{/unless}}
            {{/each}}
        </div>
        <div class="col s6 m6 l6">
            <h4 class="center-align">Favorites</h4>
            {{#each shows}}
            {{#if watched}}
            <div id={{id}}>
                <p>Title: {{title}}</p>
                <p>Seasons: {{seasons}}</p>
                <button class="remove">Remove from Favorites</button>
            </div>
            {{/if}}
            {{/each}}
            {{#if time}}
            <p>You've spent {{time}} hours watching your favorite shows!</p>
            {{/if}}
        </div>

    </div>
    <div class="row">

        <div class="col s12 m8 l8 offset-m2 offset-l2">

            <form class="new-show">
                <input id="title" placeholder="Show Title">
                <input id="seasons" placeholder="Number of Seasons">
                <input id="episodes" placeholder="Episodes per Season">
                <input id="minutes" placeholder="Minutes per Episode">
                <button type="submit" id="add-btn">Add Show To Watchlist</button>
            </form>
        </div>

    </div>


</div>